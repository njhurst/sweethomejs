/**
 * View3D interface (port of com.eteks.sweethome3d.viewcontroller.View3D, GPL v2+).
 * A view that displays a home in 3D.
 */
import type { View } from "./View.js";

export interface View3D extends View {}
